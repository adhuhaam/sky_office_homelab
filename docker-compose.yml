# Leo OS production stack
# LAN: https://192.168.18.150/ | Tailscale: http://100.126.222.96/
# Internal upstream for host nginx: 127.0.0.1:3000
services:
  postgres:
    image: postgres:17
    container_name: postgres
    restart: unless-stopped
    env_file:
      - ./postgresql/.env
    environment:
      POSTGRES_DB: appdb
      POSTGRES_USER: appuser
    volumes:
      - ./postgresql/data:/var/lib/postgresql/data
    networks:
      - homelab

  # Primary API: ASP.NET Core (Express leo-api retired; Dockerfile kept at leo-os/apps/api for rollback)
  leo-api-dotnet:
    build:
      context: ./leo-os-dotnet
      dockerfile: Dockerfile
    container_name: leo-api-dotnet
    restart: unless-stopped
    env_file:
      - ./api/.env
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      PORT: "8080"
      ASPNETCORE_URLS: http://0.0.0.0:8080
    depends_on:
      - postgres
    networks:
      - homelab

  react:
    image: nginx:alpine
    container_name: react-app
    restart: unless-stopped
    volumes:
      - ./react/app:/usr/share/nginx/html:ro
      - ./react/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - leo-api-dotnet
    networks:
      - homelab

  leo-proxy:
    image: nginx:alpine
    container_name: leo-proxy
    restart: unless-stopped
    ports:
      - "192.168.18.150:80:80"
      - "192.168.18.150:443:443"
      - "100.126.222.96:80:80"
    volumes:
      - ./infra/nginx/leo-os-docker.conf:/etc/nginx/conf.d/default.conf:ro
      - ./infra/certs:/etc/nginx/ssl:ro
    depends_on:
      - react
    networks:
      - homelab

networks:
  homelab:
    external: true
